apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pod-lifecycle.fullname" . }}-test-connection"
  labels:
    {{- include "pod-lifecycle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pod-lifecycle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
