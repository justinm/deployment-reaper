apiVersion: v1
kind: Service
metadata:
  name: {{ include "pod-lifecycle.fullname" . }}
  labels:
    {{- include "pod-lifecycle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pod-lifecycle.selectorLabels" . | nindent 4 }}
